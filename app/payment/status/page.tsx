'use client'; import {useState} from 'react'; import Link from 'next/link'; export default function Status(){const [ref,setRef]=useState('');const [data,setData]=useState<any>();async function check(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/payment-status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reference:ref})});setData(await r.json())}return <main className="mx-auto max-w-2xl px-5 py-20"><Link href="/" className="text-sm font-bold text-muted">← Home</Link><div className="card mt-8 p-8 md:p-10"><p className="text-sm font-black uppercase tracking-[.2em] text-muted">Order status</p><h1 className="mt-3 text-4xl font-black">Check your payment</h1><form onSubmit={check} className="mt-8 flex flex-col gap-3 sm:flex-row"><input className="input" placeholder="AFK-20260921-ABC123" value={ref} onChange={e=>setRef(e.target.value)}/><button className="btn btn-primary">Check Status</button></form>{data&&<div className="mt-8 rounded-3xl border p-6" style={{borderColor:'var(--line)'}}><div className="text-2xl font-black">{data.status||'PENDING'}</div><p className="mt-2 text-muted">{data.message||'Your payment submission is awaiting verification.'}</p></div>}</div></main>}
