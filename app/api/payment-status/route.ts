import {NextResponse} from 'next/server'; export async function POST(req:Request){try{const {reference}=await req.json();if(!reference||typeof reference!=='string')return NextResponse.json({error:'Enter a payment reference.'},{status:400});const url=process.env.N8N_PAYMENT_SUBMISSION_WEBHOOK_URL;if(!url)return NextResponse.json({status:'PENDING',message:'Status lookup is configured through n8n after deployment.'});const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json','X-AFK-Action':'status'},body:JSON.stringify({reference})});if(!r.ok)throw new Error();const data=await r.json();return NextResponse.json(data)}catch{return NextResponse.json({error:'Unable to check status right now.'},{status:502})}}
